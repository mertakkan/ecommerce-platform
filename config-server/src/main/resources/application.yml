server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/ecommerce-config-repo

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
